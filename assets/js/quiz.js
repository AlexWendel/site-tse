let questionNumber = 0;
let questionsList = []; 
let questionLock =false;
let totalQuestions;
let score = 0;


document.addEventListener("DOMContentLoaded", function(event) { 
	fetch('/assets/json/perguntas.json').then((e) => e.json()).then(
		(json) => {
            
        }
    );


});